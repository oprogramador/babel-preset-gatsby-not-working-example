{
  "name": "foo",
  "scripts": {
    "build": "rm -rf .cache && rm -rf public && ./node_modules/.bin/gatsby build"
  },
  "dependencies": {
    "gatsby": "^1.9.279"
  },
  "devDependencies": {
    "babel-preset-gatsby": "^0.2.29"
  }
}
